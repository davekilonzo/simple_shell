#include "header.h"

/**
 * add_numbers - Adds two numbers
 * @a: The first number
 * @b: The second number
 *
 * Return: Returns the sum
 */

int add_numbers(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
